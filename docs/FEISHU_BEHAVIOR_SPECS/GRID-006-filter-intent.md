# GRID-006 / 筛选面板操作意图

## 用户目标

打开筛选面板查看当前条件时，不因为查看或关闭面板而修改视图。

## 交互合同

- 没有筛选条件时，面板显示空状态和“添加条件”入口。
- 打开、关闭或点击面板外部均不创建规则，不触发文档保存。
- 只有用户点击“添加条件”后才创建第一条规则。
- 从字段菜单明确选择“筛选”仍可直接创建该字段的条件，因为该操作已经表达筛选意图。
- 空值条件由 `isFilterRuleActive` 继续判定为未启用，不影响记录可见性。

## 自动化

- `bitable-grid.spec.ts` 覆盖空面板零写入和显式添加条件。
